import {NativeStackScreenProps} from '@react-navigation/native-stack';

export type MainStackParams = {
  LandingScreen: undefined;
};

export type LSProps = NativeStackScreenProps<MainStackParams, 'LandingScreen'>;
